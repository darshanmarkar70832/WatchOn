<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - Details</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        /* Ensure the image scales properly */
        .product-image {
            object-fit: cover; /* Ensure the image covers the container without distortion */
        }

        /* Style the back button */
        .back-button {
            display: inline-flex;
            align-items: center;
           
            color: white; /* White text */
            padding: 8px 16px; /* Padding around text */
            border-radius: 4px; /* Rounded corners */
            border: 1px solid #4A5568; /* Matching border color */
            text-decoration: none; /* Remove underline */
            font-weight: bold; /* Bold text */
            transition: background-color 0.3s, color 0.3s; /* Smooth transitions */
        }

        .back-button:hover {
            background-color: #2D3748; /* Darker background on hover */
            color: #E2E8F0; /* Light text on hover */
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .back-button {
                margin-top: 1rem; /* Add space above the back button on smaller screens */
            }
        }

        @media (min-width: 1024px) {
            .image-container {
                max-width: 60%; /* Adjust the image width for larger screens */
            }

            .details-container {
                max-width: 40%; /* Adjust the details width for larger screens */
            }
        }
    </style>
</head>
<body class="bg-[#EDE8F5]">
    <%- include('../partials/header') %>
    <div class="flex justify-start ml-2 mt-[5rem]">
        <a class="back-button bg-blue-500" href="/shop">Back</a>
    </div>
    <div class="w-full min-h-screen flex flex-col px-6 py-6 bg-[#EDE8F5]">
        <div class="max-w-4xl mx-auto bg-white p-6 rounded-lg shadow-md">
            <div class="flex flex-col md:flex-row">
                <div class="w-full md:w-1/2 image-container">
                    <img class="w-full h-full object-cover rounded-md product-image" src="data:image/jpeg;base64,<%= product.image.toString('base64') %>" alt="<%= product.name %>">
                </div>
                <div class="md:ml-6 details-container">
                    <h2 class="text-2xl font-bold mb-2"><%= product.name %></h2>
                    <h3 class="text-xl text-gray-600 mb-4">₹ <%= product.price %></h3>
                    <p class="text-gray-800 mb-4 overflow-y-auto h-64"><%= product.details %></p>
                    <a href="/addtocart/<%= product._id %>" class="bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700">Add to Cart</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
